<Page
    xmlns:local="using:ReisProduction.Winmenu.Microsoft.UI.Xaml.Assets.Views"
    x:Class="ReisProduction.Winmenu.Microsoft.UI.Xaml.Assets.Views.ExtraFeaturesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <ScrollViewer HorizontalScrollMode="Disabled" VerticalScrollMode="Auto" Margin="0">
            <ItemsRepeater x:Name="ItemsRepeaterControl" Margin="15">
                <ItemsRepeater.Layout>
                    <UniformGridLayout
                        MinItemWidth="150"
                        MinItemHeight="200"
                        MinRowSpacing="15"
                        MinColumnSpacing="15"/>
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <Button Click="ItemButton_Click" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                                <Image Source="{Binding ImagePath}" Width="130" Height="130" Stretch="UniformToFill"/>
                                <TextBlock Text="{Binding Text}" Margin="5, 11, 5, 0" TextAlignment="Center" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>